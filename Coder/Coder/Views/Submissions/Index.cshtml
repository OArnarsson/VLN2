@model IEnumerable<Coder.Models.Entity.Submission>

@{
    ViewBag.Title = "Submissions";
}

<h1>Submissions</h1>
<div class="whiteBox" id="submissionList">
    <table class="coder-table" id="submissionTable">
        <thead>
            <tr>
                <th>ID</th>
                <th>Task</th>
                <th>Submitted</th>
                <th>Project</th>
                <th>Course</th>
                <th>Status</th>
                @if (User.IsInRole("Administrator") || ViewBag.IsTeacher)
            {
                    <th>Users</th>
                    <th>Action</th>
                }
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Id)</td>
                    <td>@Html.DisplayFor(modelItem => item.ProjectTask.Name)</td>
                    <td>@Html.DisplayFor(modelItem => item.Created)</td>
                    <td>@Html.DisplayFor(modelItem => item.ProjectTask.Project.Name)</td>
                    <td>@Html.DisplayFor(modelItem => item.ProjectTask.Project.Course.Name)</td>
                    <td> @Html.DisplayFor(modelItem => item.Status)</td>
                    @if (User.IsInRole("Administrator") || ViewBag.IsTeacher)
                    {
                        <td data-toggle="modal" data-id="@item.Id" data-target="#bs-example-modal-sm_@item.Id"><p class="btn btn-primary users-button">@item.ApplicationUsers.Count</p></td>
                        <td><a href=@Url.Action("Delete", "Submissions", new { id = item.Id })><i class="fa fa-pencil"></i></a></td>
                    }
            </tr>
        }
    </tbody>
</table>

@foreach (var item in Model)
{
    <div class="modal fade" id="bs-example-modal-sm_@item.Id" tabindex="-1" role="dialog" aria-labelledby="mySmallModalLabel">
        <div class="modal-dialog modal-sm">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    <h4 class="modal-title" id="exampleModalLabel">Users in this submission</h4>
                </div>
                <div class="modal-body">
                    <table class="coder-table submissions-table">
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Email</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var user in item.ApplicationUsers)
                            {
                                <tr>
                                    <td>@user.Name</td>
                                    <td>@user.Email</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                </div>
        </div>
    </div>
}
</div>